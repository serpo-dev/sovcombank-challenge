import React from "react";
import Window from "../_common/Window";

const Accounts = () => {
    return (
        <div>
            <Window type="accounts" />
        </div>
    );
};

export default Accounts;
