import React from "react";
import Window from "../_common/Window";

const History = () => {
    return (
        <div>
            <Window type='history' />
        </div>
    );
};

export default History;
